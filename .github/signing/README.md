# Stable debug signing

This directory is reserved for the reproducible signing material used only by the private debug application ID `com.oai.geminilivetranslate.debug`.

The debug key is intentionally not a production credential. Do not use it for the release application ID or publish it as a production signing key.
